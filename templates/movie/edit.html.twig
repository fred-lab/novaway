{% extends 'base.html.twig' %}

{% block title %}
    Editer un DVD/Bluray
{% endblock %}

{% block header %}{% endblock %}

{% block beadcrumb %}
    <nav class="breadcrumb">
        <a href="{{ path('admin_index') }}" class="breadcrumb-item">Index</a>
        <a href="{{ path('admin_movie_edit', {'movie' : movie.id}) }}" class="breadcrumb-item active">Editer ce DVD/Bluray : {{ movie.title }}</a>
    </nav>
{% endblock %}

{% block body %}
    <div class="row text-center placeholder>
        {{ include('_flash.message.html.twig') }}
    </div>
    <div class="row text-center placeholder">
        <div class="col-md-6">
            <h2>Editer un DVD/Bluray</h2>

            {{ include('movie/_form.html.twig') }}
        </div>
    </div>
{% endblock %}